apiVersion: v1
kind: Service
metadata:
  name: weather-dashboard-service
  labels:
    app: weather-dashboard
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: weather-dashboard
